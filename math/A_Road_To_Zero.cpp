#include <bits/stdc++.h>
using namespace std ; 

int main(){
    int t  ; 
    cin >> t; 
    for(int j = 0  ; j < t ; j++){
        int x,y,a,b ; 
        cin >> x >> y >> a >> b ; 
        while( x != 0 & y != 0 ){
            
        }
    }
}